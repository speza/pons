repos:
  - repo: local
    hooks:
      - id: gofmt
        name: gofmt
        entry: gofmt -w
        language: system
        types: [go]

      - id: go-check
        name: Go checks
        entry: make check
        language: system
        pass_filenames: false
        always_run: true
